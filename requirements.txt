# requirements.txt
# Dependências essenciais para o sistema de automação de controle de frota
# Administração Regional (GDF) - Decreto nº 47.091/2025

pandas>=2.2.0        # Leitura e processamento do CSV do Prime Benefícios
openpyxl>=3.1.0       # Geração/leitura de planilhas Excel (.xlsx)
docxtpl>=0.16.0        # Geração de fichas individuais em Word a partir de templates (.docx)
python-docx>=1.1.0     # Manipulação de documentos Word (usado internamente pelo docxtpl e para ajustes finos)
sqlalchemy>=2.0.0      # ORM/engine para persistência dos dados no banco SQLite